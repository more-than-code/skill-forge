name = "planner"
description = "Subtasks needing real planning, architectural decisions, or tradeoffs. Use only when judgment is the bottleneck."
model = "gpt-5.5"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Think through tradeoffs. Produce concrete recommendations with rationale.
Expect the parent task description to use `[phase]-[scope]-[task]`; if the assigned task is generic or ambiguous, ask the parent to clarify before acting.
Do not edit files or run validation commands.
Parent owns final output.
"""
