{
  "_comment": "Merge the hooks block below into ~/.claude/settings.json (or a project's .claude/settings.json). PostToolUse on Task/Agent captures subagent type, model, and task description; SessionEnd marks session boundaries; UserPromptSubmit re-states the active task's delegation mode from tasks/todo.md (silent unless it is external-worker).",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Task|Agent",
        "hooks": [
          {
            "type": "command",
            "command": "node ~/.claude/hooks/skill-forge/skill-forge-stats.mjs"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ~/.claude/hooks/skill-forge/skill-forge-stats.mjs"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ~/.claude/hooks/skill-forge/delegation-mode.mjs"
          }
        ]
      }
    ]
  }
}
